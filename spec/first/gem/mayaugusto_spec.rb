# frozen_string_literal: true

RSpec.describe First::Gem::Mayaugusto do
  it "has a version number" do
    expect(First::Gem::Mayaugusto::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
